name: Test Workflow (Python inline)

on:
  push:
    branches: [ "main" ]
    paths:
      - '_notifications/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run simple python
        run: |
          python -c "print('Hello from Python!')"
