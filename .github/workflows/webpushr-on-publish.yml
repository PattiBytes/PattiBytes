name: Test Workflow

on:
  push:
    branches: [ "main" ]
    paths:
      - '_notifications/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test Python block
        run: |
          python - <<'PY'
print("Hello from Python!")
PY
